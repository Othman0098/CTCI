static TreeNode generateMinimalTree(int[] sortedArray){
        //  Calculate the pivot to divide the array to 2 halves;
        int pivot = sortedArray.length / 2;
        
        //  Recursively call generateMinimalTree() to generate minimal tree for each half;
        ...
        ...
    }
