//  Level-Traversal of tree;

List<List<Integer>> listOfDepths(TreeNode root){
    Queue curr, next;
    
    //  Every time traverse all the nodes in curr;
    //  put all children of those nodes in next;
    //  After curr traversal finished, make curr = next, next = new queue();
    //  Begin another lever traversal until curr.isEmpty();
}
