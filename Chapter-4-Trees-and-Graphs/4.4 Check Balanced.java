//  Comparing each node`s two subtree height, if any pair differ more than one, the tree is not balanced; else balanced;

private int getHeight(TreeNode root){
    //  Return the Height of the tree(subtree) begin at root;
}
